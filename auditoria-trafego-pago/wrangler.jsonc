{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "auditoria-trafego-pago",
  "compatibility_date": "2026-06-09",
  "main": "./_worker.js",
  "assets": {
    "directory": "./",
    "binding": "ASSETS",
    "not_found_handling": "404-page"
  },
  // Domínio próprio (auditoriatrafegopago.com.br). A Cloudflare cria os registros
  // DNS e o certificado SSL automaticamente ao publicar — desde que o domínio
  // já esteja adicionado e ATIVO na mesma conta Cloudflare.
  // O redirect de www -> apex é feito em _worker.js.
  "routes": [
    { "pattern": "auditoriatrafegopago.com.br", "custom_domain": true },
    { "pattern": "www.auditoriatrafegopago.com.br", "custom_domain": true }
  ]
}
